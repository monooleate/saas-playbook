# 07 — Example: Remix + PlanetScale

> **Status:** Outline.

A Fly Machines side-app running just `node cron.js` with `node-cron`
or `croner` is the simplest setup. Trigger.dev as a hosted alternative.
