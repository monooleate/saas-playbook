# 07 — Example: Next.js + Prisma

> **Status:** Outline.

Vercel Cron (declared in `vercel.json`) hits `app/api/cron/*` routes.
Authenticated via `CRON_SECRET`. For multi-step automations, Inngest's
Vercel-native integration is the canonical choice.
