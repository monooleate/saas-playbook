# 16 — Example: SvelteKit + Supabase

> **Status:** Outline.

The Grabit pattern: changelog is `apps/landing/src/pages/changelog/`
folder of MDX files. RSS feed generated. App's `/help` modal links to
Help Scout's inbox. Feedback widget at the corner posts to a Supabase
`feedback` table that webhooks into Slack.
