# 22 — Example: SvelteKit + shadcn-svelte

> **Status:** Outline.

shadcn-svelte (Bits UI under the hood) ships with most a11y patterns
correct. Run `@axe-core/playwright` in the test suite to catch
regressions. Storybook stories double as a11y test surfaces.
