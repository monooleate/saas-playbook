# 21 — Example: SvelteKit + shadcn-svelte

> **Status:** Outline.

shadcn-svelte components committed into `apps/app/src/lib/components/ui/`
and `apps/landing/src/components/ui/` via the same generator. Tailwind
config shared via a workspace package.
