# 18 — Example: Next.js + Prisma

> **Status:** Outline.

Hono on Vercel Edge for the API surface (faster cold starts than
Next's API routes). `Authorization: Bearer` middleware. Zod schemas →
OpenAPI via `@hono/zod-openapi`. Webhooks via Inngest's webhook
fan-out feature.
