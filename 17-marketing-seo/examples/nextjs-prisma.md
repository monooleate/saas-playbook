# 17 — Example: Next.js + Prisma

> **Status:** Outline.

Marketing pages in `app/(marketing)/...`, app pages in `app/(app)/...`.
Or separate `apps/marketing` Next.js workspace. OG images via Vercel
OG (`@vercel/og`). Sitemap via `app/sitemap.ts`. Programmatic SEO via
generateStaticParams in dynamic segments.
