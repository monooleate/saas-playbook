# 06 — Example: Remix + PlanetScale

> **Status:** Outline.

Pattern: Remix loaders in `app/root.tsx` fetch onboarding state, render
a banner if incomplete. Drip emails via Fly machine cron + Postmark
templates.
