# 06 — Example: SvelteKit + Supabase

> **Status:** Outline.

The Grabit onboarding flow: signup → verify → workspace creation
(name, subdomain, country) → first-resource creation guided. Setup
checklist persists in `tenant.onboarding_state` jsonb. Email drip via
Resend triggered by Supabase database webhooks.

Full walkthrough in v0.2.
