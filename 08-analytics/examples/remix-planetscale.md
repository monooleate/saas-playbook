# 08 — Example: Remix + PlanetScale

> **Status:** Outline.

PostHog server SDK. Customer-facing reports via Drizzle views and
Recharts. PDF via `@react-pdf/renderer` or a separate Python service
(WeasyPrint) on Fly Machines.
