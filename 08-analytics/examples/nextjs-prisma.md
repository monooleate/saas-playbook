# 08 — Example: Next.js + Prisma

> **Status:** Outline.

PostHog Next.js SDK (`posthog-js` + `posthog-node`). Vercel Analytics
free tier covers Web Vitals. Reports via React Server Components +
Recharts; CSV export streams via the Web Streams API; PDF via
`@react-pdf/renderer` (in-process, no Puppeteer).
