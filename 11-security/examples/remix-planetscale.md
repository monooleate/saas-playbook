# 11 — Example: Remix + PlanetScale

> **Status:** Outline.

Fly's edge proxy adds basic security headers; add CSP via Remix's
`entry.server.tsx` response writer. Drizzle uses parameterized queries.
PlanetScale's connection string includes `sslaccept=strict` — required.
