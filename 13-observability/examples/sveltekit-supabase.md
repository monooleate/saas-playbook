# 13 — Example: SvelteKit + Supabase

> **Status:** Outline.

Sentry SvelteKit SDK (`@sentry/sveltekit`) wraps both server and
client. Logs shipped via `pino` to BetterStack from PM2 (configured in
`ecosystem.config.cjs`). UptimeRobot hits `/api/healthz`.
