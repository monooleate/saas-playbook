# 13 — Example: Remix + PlanetScale

> **Status:** Outline.

`@sentry/remix`. Logs shipped from Fly Machines via Vector → Axiom.
Fly's healthcheck (configured in `fly.toml`) pulls double-duty as
internal liveness and external uptime ping.
