# 09 — Example: Next.js + Prisma

> **Status:** Outline.

next-intl with App Router. Strings in `messages/{locale}.json`. Routing
via `[locale]` segment + middleware. Server components and client
components have different APIs (`getTranslations` vs `useTranslations`).
