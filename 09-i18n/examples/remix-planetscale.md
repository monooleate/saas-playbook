# 09 — Example: Remix + PlanetScale

> **Status:** Outline.

`remix-i18next` (a Remix-specific i18next wrapper). Locale resolved in
the root loader from URL prefix or `Accept-Language` fallback. Strings
in `public/locales/{lng}/{namespace}.json`.
