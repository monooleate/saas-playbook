# 09 — Example: SvelteKit + Supabase

> **Status:** Outline.

Paraglide JS is the SvelteKit-native i18n library. The Grabit pattern:
strings in `messages/{locale}.json`, type-safe `m.greeting()` calls in
components, locale resolved from the URL prefix in
`hooks.server.ts`.
