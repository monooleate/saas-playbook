# 15 — Example: Next.js + Prisma

> **Status:** Outline.

Static MDX pages for legal docs at `/terms`, `/privacy`. Prisma
`Consent` model. `/api/me/export` returns a streamed JSON of all user
data. `/api/me/delete` schedules a 30-day delayed hard-delete.
