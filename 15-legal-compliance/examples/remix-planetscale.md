# 15 — Example: Remix + PlanetScale

> **Status:** Outline.

Static markdown rendered through Remix loaders. Drizzle `consents`
table. `/api/me/export.json` and `/api/me/delete` with email
confirmation pattern.
