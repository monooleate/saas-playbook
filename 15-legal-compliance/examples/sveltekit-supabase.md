# 15 — Example: SvelteKit + Supabase

> **Status:** Outline.

`/terms`, `/privacy`, `/subprocessors`, `/dpa` are static pages on the
Astro marketing site (see topic 17). Account-side: `/api/me/export` and
`/api/me/delete` endpoints in the SvelteKit app. Consent records in
`tenant_consents(tenant_id, doc, version, accepted_at, ip)`.
